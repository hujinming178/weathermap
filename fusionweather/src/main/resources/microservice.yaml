servicecomb:
  service:
    application: weathermap
    name: fusionweather
    version: 0.0.2
    properties:
      allowCrossApp: false
    registry:
      address: https://192.168.0.211:30100,https://192.168.0.241:30100 #此处填写要注册到的微服务引擎的注册中心地址
      instance:
        watch: false
        empty:
          protection: false
        
  kie:
    serverUri: https://192.168.0.211:30110,https://192.168.0.241:30110 #此处填写要注册到的微服务引擎的配置中心地址
    refreshMode: 1
    refresh_interval: 5000

  rest:
    address: 0.0.0.0:13092
  request:
    timeout: 3000
  handler:
    chain:
      Provider:
        default: qps-flowcontrol-provider,bizkeeper-provider
      Consumer:
        default: qps-flowcontrol-consumer,loadbalance,bizkeeper-consumer,fault-injection-consumer,auth-consumer
  accesslog:
    enabled: true
    pattern: "%h %{dd/MMM/yyyy:HH:mm:ss.SSSZ|GMT+0008|en_US}t %r %s %B %D"

# TODO: 使用微服务引擎专业版部署之前，需要配置 AK/SK
#  credentials:
#    akskEnabled: true
#    accessKey: add your ak/sk from huaweicloud
#    secretKey: add your ak/sk from huaweicloud
#    akskCustomCipher: default
#    project: cn-south-1
